import BookShell from "@/components/BookShell";

export default function Home() {
  return (
    <main className="min-h-screen w-full bg-[var(--void)] flex items-center justify-center">
      <BookShell />
    </main>
  );
}
